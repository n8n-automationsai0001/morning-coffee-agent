__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.tmp
briefing.png

# Secrets — injected as CCR environment variables, not committed
.env
shared/token.json
shared/token.json.bak

# Local auto-memory mirrors (Claude Code's per-project memory system)
MEMORY.md
memory/
